import type { EdgeTypes } from '@xyflow/react';
import { FuseEdge } from './fuse-edge';
import { SubtopicFuseEdge } from './subtopic-fuse-edge';

export const edgeTypes: EdgeTypes = {
  fuseEdge: FuseEdge,
  subtopicFuseEdge: SubtopicFuseEdge,
};
