import type { NodeTypes } from '@xyflow/react';
import { TopicNode } from './topic-node';
import { SubtopicNode } from './subtopic-node';

export const nodeTypes: NodeTypes = {
  topicNode: TopicNode,
  subtopicNode: SubtopicNode,
};
